pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                echo 'Checking out source code...'
            }
        }

        stage('Build') {
            steps {
                echo 'Building the application...'
                bat 'cd app\\devsecops-app && mvn clean package -DskipTests'
            }
        }

    }
}